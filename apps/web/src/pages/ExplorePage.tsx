import { useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { events } from '../data/events';
import { Icon } from '../components/Icon';

export default function ExplorePage() {
  const [search, setSearch] = useState(''); const [category, setCategory] = useState('All'); const [location, setLocation] = useState('All locations');
  const categories = ['All', 'Concerts', 'Comedy', 'Sports', 'Parties', 'Theatre', 'Conferences'];
  const results = useMemo(() => events.filter((event) => (category === 'All' || event.category === category) && (location === 'All locations' || event.location.includes(location)) && `${event.title} ${event.location}`.toLowerCase().includes(search.toLowerCase())), [search, category, location]);
  return <div className="min-h-screen bg-[#faf9fc] text-gray-900"><nav className="border-b border-purple-100 bg-white"><div className="mx-auto flex h-[72px] max-w-7xl items-center justify-between px-4 sm:px-6"><Link to="/" className="text-3xl font-black tracking-[-.08em] text-purple-800">OTIKO<span className="text-amber-500">.</span></Link><div className="flex gap-5 text-sm font-semibold text-gray-600"><Link to="/" className="hover:text-purple-700">Home</Link><Link to="/apply-organizer" className="text-purple-700">For organisers</Link></div></div></nav><header className="bg-gradient-to-r from-purple-800 via-indigo-700 to-blue-700 py-16 text-white"><div className="mx-auto max-w-7xl px-4 sm:px-6"><p className="text-sm font-bold uppercase tracking-[.16em] text-amber-300">Find your moment</p><h1 className="mt-3 text-4xl font-black tracking-tight sm:text-5xl">Explore events across Kenya</h1><div className="mt-8 grid gap-3 rounded-2xl bg-white p-3 shadow-xl md:grid-cols-[1.4fr_1fr_auto]"><label className="flex items-center gap-2 rounded-xl bg-gray-50 px-4 py-3 text-gray-400"><Icon name="search" className="h-5 w-5" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search artists, events or venues" className="w-full bg-transparent text-sm text-gray-900 outline-none" /></label><select value={location} onChange={(e) => setLocation(e.target.value)} className="rounded-xl bg-gray-50 px-4 py-3 text-sm font-semibold text-gray-700 outline-none"><option>All locations</option><option>Nairobi</option><option>Mombasa</option><option>Kisumu</option></select><button className="rounded-xl bg-amber-500 px-6 py-3 text-sm font-bold text-white">Search</button></div></div></header><main className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><div className="flex flex-wrap gap-2">{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`rounded-full px-4 py-2 text-sm font-bold transition ${category === item ? 'bg-purple-700 text-white shadow-lg' : 'bg-white text-gray-600 ring-1 ring-gray-200 hover:bg-purple-50'}`}>{item}</button>)}</div><div className="mt-10 flex items-end justify-between"><div><p className="text-sm font-bold uppercase tracking-[.14em] text-amber-600">Upcoming</p><h2 className="mt-2 text-3xl font-black tracking-tight">Events for you</h2></div><p className="text-sm text-gray-500">{results.length} events found</p></div><div className="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{results.map((event) => <Link key={event.id} to={`/events/${event.id}`} className="group overflow-hidden rounded-3xl bg-white shadow-[0_12px_30px_rgba(45,26,78,.09)] transition hover:-translate-y-1 hover:shadow-xl"><div className="relative h-52 overflow-hidden"><img src={event.image} alt={event.title} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1 text-xs font-bold text-purple-700">{event.category}</span></div><div className="p-5"><p className="text-xs font-bold uppercase tracking-[.12em] text-amber-600">{event.date}</p><h3 className="mt-2 text-xl font-extrabold group-hover:text-purple-700">{event.title}</h3><p className="mt-2 flex items-center gap-1.5 text-sm text-gray-500"><Icon name="pin" className="h-4 w-4 text-purple-600" />{event.location}</p><div className="mt-5 flex items-center justify-between"><span className="font-bold text-purple-700">From KSh {event.price.toLocaleString()}</span><Icon name="arrow" className="h-5 w-5 text-purple-700" /></div></div></Link>)}</div>{!results.length && <p className="py-20 text-center text-gray-500">No events match those filters.</p>}</main></div>;
}
